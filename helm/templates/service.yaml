apiVersion: v1
kind: Service
metadata:
  name: {{ include "javaTemplate.fullname" . }}
  labels:
    {{- include "javaTemplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javaTemplate.selectorLabels" . | nindent 4 }}
